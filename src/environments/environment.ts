// Metodo que configura la integracion con el servidor
export const environment = {
  production: false,
  URL_BASE: 'http://localhost:8080/',
  http_options: {
    'Content-Type': 'application/json'
  }
};

